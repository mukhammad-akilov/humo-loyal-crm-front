import {CreatePaymentProps} from "./CreatePayment.props";

const CreatePayment = ({}: CreatePaymentProps): JSX.Element => {
    return (
        <div>Create payment component</div>
    )
}

export default CreatePayment;