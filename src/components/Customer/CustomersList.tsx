import {CustomersListProps} from "./CustomersList.props";

const CustomersList = ({...restProps}: CustomersListProps): JSX.Element => {
    return (
        <div>Customers list</div>
    )
}

export default CustomersList;